package library.controls;

/**
 * The StatsControl class represents a record with no additional properties or methods.
 * This minimal implementation serves as a placeholder or defaults to signify a minimal unit of control or state tracking.
 */
public record StatsControl() {
}
